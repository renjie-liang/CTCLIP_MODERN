_base_: ../base_config.yaml

experiment:
  name: "ctclip_ablation_swiglu_only"

model:
  image_encoder:
    # Only SwiGLU enabled
    use_flash_attention: false
    use_rms_norm: false
    use_swiglu: true

checkpoint:
  save_dir: "saves/ablation_swiglu_only"

logging:
  wandb_project: "ctclip_ablation"
  wandb_run_name: "swiglu_only"
  wandb_tags: ["ablation", "swiglu"]
