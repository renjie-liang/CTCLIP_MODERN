_base_: ../base_config.yaml

experiment:
  name: "ctclip_optimized_full"

model:
  image_encoder:
    # Full optimizations - all enabled
    use_flash_attention: true
    use_rms_norm: true
    use_swiglu: true

checkpoint:
  save_dir: "saves/optimized_full"

logging:
  wandb_project: "ctclip_ablation"
  wandb_run_name: "optimized_full"
  wandb_tags: ["optimized", "flash_attention", "rms_norm", "swiglu"]
