_base_: ../base_config.yaml

experiment:
  name: "ctclip_ablation_flash_rms"

model:
  image_encoder:
    # FlashAttention + RMSNorm (no SwiGLU)
    use_flash_attention: true
    use_rms_norm: true
    use_swiglu: false

checkpoint:
  save_dir: "saves/ablation_flash_rms"

logging:
  wandb_project: "ctclip_ablation"
  wandb_run_name: "flash_rms"
  wandb_tags: ["ablation", "flash_attention", "rms_norm"]
