_base_: ../base_config.yaml

experiment:
  name: "ctclip_ablation_flash_only"

model:
  image_encoder:
    # Only FlashAttention enabled
    use_flash_attention: true
    use_rms_norm: false
    use_swiglu: false

checkpoint:
  save_dir: "saves/ablation_flash_only"

logging:
  wandb_project: "ctclip_ablation"
  wandb_run_name: "flash_only"
  wandb_tags: ["ablation", "flash_attention"]
