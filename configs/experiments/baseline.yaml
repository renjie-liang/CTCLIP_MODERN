_base_: ../base_config.yaml

experiment:
  name: "ctclip_baseline"

model:
  image_encoder:
    # Baseline configuration - all optimizations disabled
    use_flash_attention: false
    use_rms_norm: false
    use_swiglu: false

checkpoint:
  save_dir: "saves/baseline"

logging:
  wandb_project: "ctclip_ablation"
  wandb_run_name: "baseline"
  wandb_tags: ["baseline", "no_optimization"]
